# hu - Hugo Utilities

A command-line utility to automate tasks for Hugo static site generator projects.

## Installation

```bash
# Install from GitHub
uv pip install git+https://github.com/beint-no/hu.git

# Or install from local directory
uv pip install -e .
```

## Assumption

All commands assume they are done from the root of the hugo project