You are writing a modern CLI for handling hugo static site generator repositories. 

Tools like: 
* Find broken links (404)
* generate SVG feature image based on frontmatter values

All commands are executed from the root of the repo 